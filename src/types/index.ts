export type Option = { label: string | number; value: string | number };

export type Fruit = { name: string; color: string; score: number };

export type Car = { name: string; price: number; discount: number; desirable: boolean };
